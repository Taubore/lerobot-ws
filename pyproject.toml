[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "lerobot-ws"
version = "0.1.0"
description = "Scripts d'apprentissage LeRobot"
requires-python = ">=3.12"

[tool.setuptools]
packages = ["commun"]